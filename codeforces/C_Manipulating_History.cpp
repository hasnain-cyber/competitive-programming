#include <bits/stdc++.h>

typedef long long ll;
typedef long double ld;

#define MOD 1000000007
#define MIN numeric_limits<ll>::min()
#define MAX numeric_limits<ll>::max()

using namespace std;

string solve(string s, vector<pair<string, string>> operations, ll index, ll n) {
    if (s.find(operations[index].first)) {
        s.rep
    } else {
        return "";
    }
}

int main() {
    ll t;
    cin >> t;
    while (t--) {
        ll n;
        cin >> n;
        vector<pair<string, string>> operations;
        for (ll i = 0; i < 2 * n; i++) {
            pair<string, string> temp;
            cin >> temp.second >> temp.first;
        }

        string final_string;
        cin >> final_string;
    }
}