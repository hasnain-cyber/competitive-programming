#include <bits/stdc++.h>

typedef long long int num;

using namespace std;

int main() {
    num n, m, k;
    cin >> n >> m >> k;
    num arr[m];
    for (num i = 0; i < m; i++)
        cin >> arr[i];
}

// could not solve